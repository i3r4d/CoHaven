
import { Navigate } from "react-router-dom";
import Landing from "./Landing";

// This component serves as the entry point and redirects to our Landing page
const Index = () => {
  return <Landing />;
};

export default Index;
